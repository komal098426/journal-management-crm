import {
  Activity, BarChart3, Bell, BookOpen, CalendarDays, Check, CheckCircle2, ChevronDown,
  ClipboardCheck, Clock3, Filter, Grid2X2, LayoutDashboard, ListTodo, Menu, MoreHorizontal,
  Plus, Search, Settings2, SlidersHorizontal, Sparkles, Users, X, Zap
} from "lucide-react";
import { useMemo, useState } from "react";

type Employee = { id: number; name: string; initials: string; employeeId: string; department: string; role: string; hours: string; status: "Active" | "Low stock alerts" | "Offline"; color: string; email: string; phone: string };
type Operations = { id: number; employee: string; initials: string; color: string; department: string; work: string; time: string; status: "Completed" | "In Progress" | "Pending" | "Cancelled"; date: string; description: string };
type Task = { id: number; title: string; employee: string; department: string; due: string; priority: "High" | "Medium" | "Low"; status: "Completed" | "In Progress" | "Pending" };

type Page = "ERP Overview" | "People" | "Operations" | "Attendance" | "Inventory" | "Procurement" | "Finance" | "Reports & Analytics" | "Settings";

const employeesSeed: Employee[] = [
  { id: 1, name: "Ahmed Khan", initials: "AK", employeeId: "EMP-024", department: "Laboratory", role: "Lab Technician", hours: "09:00 – 17:00", status: "Active", color: "#d7b9a6", email: "ahmed.khan@northstar.test", phone: "+92 300 123 4521" },
  { id: 2, name: "Sara Ali", initials: "SA", employeeId: "EMP-018", department: "Research", role: "Research Associate", hours: "10:00 – 18:00", status: "Active", color: "#c4bced", email: "sara.ali@northstar.test", phone: "+92 301 884 2310" },
  { id: 3, name: "Bilal Hussain", initials: "BH", employeeId: "EMP-031", department: "Administration", role: "Office Manager", hours: "08:30 – 16:30", status: "Active", color: "#bad2b4", email: "bilal.h@northstar.test", phone: "+92 303 442 1059" },
  { id: 4, name: "Nadia Raza", initials: "NR", employeeId: "EMP-009", department: "Accounts", role: "Accounts Officer", hours: "09:00 – 17:00", status: "Low stock alerts", color: "#e4c494", email: "nadia.raza@northstar.test", phone: "+92 311 762 4892" },
  { id: 5, name: "Omar Farooq", initials: "OF", employeeId: "EMP-042", department: "Sales", role: "Account Executive", hours: "09:30 – 17:30", status: "Active", color: "#b9c6d2", email: "omar.farooq@northstar.test", phone: "+92 335 762 4992" },
  { id: 6, name: "Maryam Saeed", initials: "MS", employeeId: "EMP-014", department: "Laboratory", role: "Quality Analyst", hours: "09:00 – 17:00", status: "Offline", color: "#d5b9c9", email: "maryam.s@northstar.test", phone: "+92 312 821 3434" },
];

const journalSeed: Operations[] = [
  { id: 1, employee: "Ahmed Khan", initials: "AK", color: "#d7b9a6", department: "Laboratory", work: "Sample Analysis", time: "09:00 – 11:30", status: "Completed", date: "09 Sep 2026", description: "Processed the morning batch and uploaded test results for review." },
  { id: 2, employee: "Sara Ali", initials: "SA", color: "#c4bced", department: "Research", work: "Report Preparation", time: "11:00 – 13:00", status: "In Progress", date: "09 Sep 2026", description: "Preparing the comparative report for the Q3 research review." },
  { id: 3, employee: "Bilal Hussain", initials: "BH", color: "#bad2b4", department: "Administration", work: "Vendor Follow-up", time: "13:30 – 14:15", status: "Pending", date: "09 Sep 2026", description: "Confirming delivery windows with the three approved vendors." },
  { id: 4, employee: "Omar Farooq", initials: "OF", color: "#b9c6d2", department: "Sales", work: "Client Check-in", time: "15:00 – 16:00", status: "Completed", date: "08 Sep 2026", description: "Walkthrough of the new service package with the Northstar account." },
  { id: 5, employee: "Maryam Saeed", initials: "MS", color: "#d5b9c9", department: "Laboratory", work: "Calibration Review", time: "10:00 – 12:00", status: "Cancelled", date: "08 Sep 2026", description: "Moved to next week's calibration block due to equipment maintenance." },
];

const taskSeed: Task[] = [
  { id: 1, title: "Approve sample batch results", employee: "Ahmed Khan", department: "Laboratory", due: "Today, 16:00", priority: "High", status: "In Progress" },
  { id: 2, title: "Finalize Q3 research report", employee: "Sara Ali", department: "Research", due: "10 Sep 2026", priority: "High", status: "In Progress" },
  { id: 3, title: "Update vendor register", employee: "Bilal Hussain", department: "Administration", due: "12 Sep 2026", priority: "Medium", status: "Pending" },
  { id: 4, title: "Send renewal summary", employee: "Omar Farooq", department: "Sales", due: "08 Sep 2026", priority: "Low", status: "Completed" },
];

const departments = [
  { name: "Laboratory", code: "LAB", count: 12, head: "Ahmed Khan", color: "#c9c0ff", detail: "Testing, analysis & quality" },
  { name: "Research", code: "R&D", count: 8, head: "Sara Ali", color: "#bfe2b6", detail: "Insights & development" },
  { name: "Administration", code: "ADM", count: 6, head: "Bilal Hussain", color: "#f0d29b", detail: "Office operations" },
  { name: "Accounts", code: "ACC", count: 4, head: "Nadia Raza", color: "#e9b9b5", detail: "Finance & reporting" },
  { name: "Sales", code: "SAL", count: 7, head: "Omar Farooq", color: "#b9c9d8", detail: "Client relationships" },
];

const nav: { label: Page; icon: typeof LayoutDashboard }[] = [
  { label: "ERP Overview", icon: LayoutDashboard }, { label: "People", icon: Users }, { label: "Operations", icon: BookOpen },
  { label: "Attendance", icon: ClipboardCheck }, { label: "Inventory", icon: Grid2X2 }, { label: "Procurement", icon: ListTodo },
  { label: "Finance", icon: BarChart3 }, { label: "Reports & Analytics", icon: Activity }, { label: "Settings", icon: Settings2 },
];

function StatusBadge({ status }: { status: string }) {
  const variant = status === "Completed" || status === "Present" || status === "Active" ? "completed" : status === "In Progress" || status === "Late" ? "progress" : status === "Low stock alerts" || status === "Leave" || status === "Cancelled" ? "leave" : "pending";
  return <span className={`status-badge ${variant}`}>{status}</span>;
}

function Avatar({ initials, color, size = "normal" }: { initials: string; color: string; size?: "normal" | "large" }) {
  return <span className={`avatar ${size === "large" ? "avatar-large" : ""}`} style={{ background: color }}>{initials}</span>;
}

function StatCard({ label, value, foot, accent, icon: Icon }: { label: string; value: string; foot: string; accent: string; icon: typeof Users }) {
  return <div className="glass-card stat-card" style={{ "--card-accent": accent } as React.CSSProperties}><div className="stat-label">{label}</div><div className="stat-value">{value}</div><div className="stat-foot"><Icon size={12} /> <span>{foot}</span></div></div>;
}

function Dashboard({ go }: { go: (page: Page) => void }) {
  const bars = [45, 68, 53, 83, 72, 98, 80, 65, 89, 77, 92, 70];
  return <>
    <div className="page-heading"><div><div className="eyebrow">Wednesday · 09 September 2026 · ERP CONTROL CENTER</div><h1>Good morning, Ayesha</h1><p>One connected view of people, operations, inventory, procurement, and finance across Northstar.</p></div><div className="heading-actions"><button className="icon-button" aria-label="notifications"><Bell size={15} /></button><button className="primary-button" onClick={() => go("Operations")}><Plus size={14} /> New record</button></div></div>
    <div className="stats-grid">
      <StatCard label="Active workforce" value="37" foot="37 active users" accent="rgba(197,187,255,.28)" icon={Users} />
      <StatCard label="Open operations" value="24" foot="18 orders in flow" accent="rgba(191,227,181,.22)" icon={Activity} />
      <StatCard label="Low stock alerts" value="03" foot="4 items need reorder" accent="rgba(239,209,151,.25)" icon={CalendarDays} />
      <StatCard label="Today's revenue" value="92%" foot="+8.4% this month" accent="rgba(225,186,182,.22)" icon={CheckCircle2} />
    </div>
    <div className="content-grid">
      <div className="panel"><div className="panel-header"><div><div className="panel-title">Business activity</div><div className="panel-subtitle">Orders, stock movements & work logs · 01–09 September</div></div><div className="legend"><span><i /> Orders processed</span><span><i className="purple" /> Procurement</span></div></div><div className="activity-graph">{bars.map((height, i) => <div className="graph-column" key={i}><div className="graph-bar" style={{ height: `${height}%` }} /><div className="graph-bar alt" style={{ height: `${Math.max(22, height - 28)}%` }} /><span className="graph-label">{["01","02","03","04","05","06","07","08","09","10","11","12"][i]}</span></div>)}</div></div>
      <div className="panel today-card"><div className="panel-header"><div><div className="panel-title">Today's operations pulse</div><div className="panel-subtitle">09 September · live</div></div><Zap size={15} color="#f0dca9" /></div><div className="today-ring"><div className="today-ring-content"><b>76%</b><span>system health</span></div></div><div className="today-summary"><div><strong>18</strong><span>orders processed</span></div><div><strong>05</strong><span>purchase orders</span></div><div><strong>24</strong><span>users online</span></div></div></div>
    </div>
    <div className="content-grid" style={{ marginTop: 10 }}>
      <div className="panel"><div className="panel-header"><div><div className="panel-title">Latest business activity</div><div className="panel-subtitle">Recent movements across connected ERP modules</div></div><button className="secondary-button" onClick={() => go("Operations")}>View operations <ChevronDown size={12} /></button></div><div className="timeline">{journalSeed.slice(0, 4).map(item => <div className="timeline-row" key={item.id}><span className="timeline-time">{item.time.split(" ")[0]}</span><Avatar initials={item.initials} color={item.color} /><div className="timeline-copy"><b>{item.employee} <span style={{ color: "#9c9a94", fontWeight: 400 }}>· {item.department}</span></b><span>{item.work}</span></div><StatusBadge status={item.status} /></div>)}</div></div>
      <div className="panel"><div className="panel-header"><div><div className="panel-title">Procurement overview</div><div className="panel-subtitle">A quick view of today's enterprise workload</div></div><button className="icon-button" onClick={() => go("Procurement")}><MoreHorizontal size={15} /></button></div><div className="mini-metric" style={{ marginBottom: 9 }}><span>Revenue this month</span><b>48 <small style={{ fontSize: 11, color: "#b7d9af" }}>+12%</small></b></div><div className="mini-metric"><span>Open approvals</span><b>09 <small style={{ fontSize: 11, color: "#f0d49b" }}>due soon</small></b></div></div>
    </div>
  </>;
}

function PeoplePage({ employees, onAdd, query, setQuery }: { employees: Employee[]; onAdd: () => void; query: string; setQuery: (value: string) => void }) {
  const filtered = employees.filter(item => `${item.name} ${item.employeeId} ${item.department}`.toLowerCase().includes(query.toLowerCase()));
  return <div className="panel data-panel page-panel"><div className="panel-header"><div><div className="panel-title">People</div><div className="panel-subtitle">37 team members across 5 work areas</div></div><button className="primary-button" onClick={onAdd}><Plus size={14} /> Add employee</button></div><div className="toolbar"><div className="search-box"><Search size={14} /><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Search name, ID or department" /></div><button className="secondary-button"><Filter size={13} /> Filter</button><select className="select-dark" defaultValue="all"><option value="all">All departments</option><option>Laboratory</option><option>Research</option><option>Sales</option></select></div><div className="table-wrap"><table><thead><tr><th>Employee</th><th>Department</th><th>Working hours</th><th>Status</th><th>Contact</th><th /></tr></thead><tbody>{filtered.map(item => <tr key={item.id}><td><div className="person-cell"><Avatar initials={item.initials} color={item.color} /><div><b>{item.name}</b><span>{item.employeeId} · {item.role}</span></div></div></td><td>{item.department}</td><td className="mono">{item.hours}</td><td><StatusBadge status={item.status} /></td><td className="mono">{item.email}</td><td><button className="icon-button" style={{ width: 30, height: 30 }}><MoreHorizontal size={14} /></button></td></tr>)}</tbody></table>{filtered.length === 0 && <div className="empty-state">No team members match this search.</div>}</div></div>;
}

function OperationsPage({ entries, onAdd }: { entries: Operations[]; onAdd: () => void }) {
  return <div className="panel data-panel page-panel"><div className="panel-header"><div><div className="panel-title">Operations / business records</div><div className="panel-subtitle">The connected record of work, orders, stock, and approvals across Northstar</div></div><button className="primary-button" onClick={onAdd}><Plus size={14} /> Add record</button></div><div className="toolbar"><div className="search-box"><Search size={14} /><input placeholder="Search orders, records, people or notes" /></div><button className="secondary-button"><CalendarDays size={13} /> 09 Sep 2026</button><button className="secondary-button"><SlidersHorizontal size={13} /> Status</button></div><div className="timeline" style={{ padding: "7px 20px 20px" }}>{entries.map(item => <div key={item.id} className="journal-record"><div className="journal-date">{item.date}</div><div className="timeline-row" style={{ padding: "15px 0", borderTop: "1px solid rgba(255,255,255,.08)" }}><span className="timeline-time">{item.time}</span><Avatar initials={item.initials} color={item.color} /><div className="timeline-copy"><b>{item.work}</b><span>{item.employee} · {item.department} · {item.description}</span></div><StatusBadge status={item.status} /></div></div>)}</div></div>;
}

function ProcurementPage({ tasks, setProcurement, onAdd }: { tasks: Task[]; setProcurement: (tasks: Task[]) => void; onAdd: () => void }) {
  const advance = (id: number) => setProcurement(tasks.map(item => item.id === id ? { ...item, status: item.status === "Pending" ? "In Progress" : item.status === "In Progress" ? "Completed" : "Pending" } : item));
  return <div className="panel data-panel page-panel"><div className="panel-header"><div><div className="panel-title">Procurement</div><div className="panel-subtitle">Keep ownership, priority and due dates visible</div></div><button className="primary-button" onClick={onAdd}><Plus size={14} /> New task</button></div><div className="metric-grid"><div className="mini-metric"><span>Open tasks</span><b>09</b></div><div className="mini-metric"><span>Due today</span><b>05</b></div><div className="mini-metric"><span>Completion rate</span><b>84%</b></div></div><div className="table-wrap"><table><thead><tr><th>Task</th><th>Owner</th><th>Due date</th><th>Priority</th><th>Status</th><th /></tr></thead><tbody>{tasks.map(item => <tr key={item.id}><td><b>{item.title}</b><div className="panel-subtitle">{item.department}</div></td><td>{item.employee}</td><td className="mono">{item.due}</td><td><StatusBadge status={item.priority} /></td><td><button className="status-button" onClick={() => advance(item.id)}><StatusBadge status={item.status} /></button></td><td><MoreHorizontal size={15} color="#aaa8a2" /></td></tr>)}</tbody></table></div></div>;
}

function AttendancePage() {
  const rows = employeesSeed.slice(0, 5).map((item, i) => ({ ...item, attendance: ["Present", "Late", "Present", "Leave", "Present"][i], check: ["08:57", "10:14", "08:45", "—", "09:28"][i] }));
  return <div className="panel data-panel page-panel"><div className="panel-header"><div><div className="panel-title">Attendance</div><div className="panel-subtitle">Wednesday, 09 September 2026 · 92% team system health</div></div><button className="secondary-button"><CalendarDays size={13} /> This month <ChevronDown size={12} /></button></div><div className="metric-grid"><div className="mini-metric"><span>Present</span><b style={{ color: "#c9e4bf" }}>31</b></div><div className="mini-metric"><span>Late / half day</span><b style={{ color: "#f0d49b" }}>03</b></div><div className="mini-metric"><span>Low stock alerts</span><b style={{ color: "#efb7ae" }}>03</b></div></div><div className="table-wrap"><table><thead><tr><th>Employee</th><th>Department</th><th>Check-in</th><th>Attendance</th><th>Month overview</th></tr></thead><tbody>{rows.map(item => <tr key={item.id}><td><div className="person-cell"><Avatar initials={item.initials} color={item.color} /><b>{item.name}</b></div></td><td>{item.department}</td><td className="mono">{item.check}</td><td><StatusBadge status={item.attendance} /></td><td><div style={{ display: "flex", gap: 3 }}>{Array.from({ length: 10 }).map((_, i) => <i key={i} style={{ display: "block", width: 10, height: 10, borderRadius: 3, background: i === 5 && item.attendance === "Leave" ? "#efb7ae" : i === 2 && item.attendance === "Late" ? "#f0d49b" : "#c9e4bf" }} />)}</div></td></tr>)}</tbody></table></div></div>;
}

function InventoryPage() {
  const days = ["Mon 07", "Tue 08", "Wed 09", "Thu 10", "Fri 11", "Sat 12"];
  return <div className="panel data-panel page-panel"><div className="panel-header"><div><div className="panel-title">Work schedule</div><div className="panel-subtitle">Weekly timetable · 07–12 September 2026</div></div><button className="primary-button"><Plus size={14} /> Assign hours</button></div><div style={{ overflowX: "auto" }}><div className="week-grid"><div className="week-head">TEAM</div>{days.map(day => <div className="week-head" key={day}>{day}</div>)}{["09:00", "11:00", "13:00", "15:00"].map((time, row) => <div key={time} className="mono">{time}</div>)}</div></div><div className="week-grid" style={{ marginTop: -20 }}><div className="schedule-block" style={{ gridRow: "1 / span 2", background: "rgba(201,192,255,.17)" }}><b>Ahmed</b><br /><small>Laboratory</small></div><div className="schedule-block" style={{ gridColumn: "2 / span 5" }}><b>Core team hours</b><br /><small>09:00 – 17:00 · 24 working today</small></div></div></div>;
}

function FinancePage() {
  return <div className="panel data-panel page-panel"><div className="panel-header"><div><div className="panel-title">Finance / work areas</div><div className="panel-subtitle">Five focused teams keep the organization moving</div></div><button className="primary-button"><Plus size={14} /> Add department</button></div><div className="dept-grid">{departments.map(item => <div className="dept-card" key={item.code}><span className="dept-color" style={{ background: item.color }} /><h3>{item.name}</h3><p>{item.detail}</p><footer><span>{item.head}</span><b>{item.count} staff</b></footer></div>)}</div></div>;
}

function ReportsPage() {
  return <div className="panel data-panel page-panel"><div className="panel-header"><div><div className="panel-title">Reports & analytics</div><div className="panel-subtitle">A clear read on productivity, system health and momentum</div></div><button className="secondary-button"><CalendarDays size={13} /> This month <ChevronDown size={12} /></button></div><div className="metric-grid"><div className="mini-metric"><span>Hours logged</span><b>1,284</b></div><div className="mini-metric"><span>Procurement completed</span><b>184</b></div><div className="mini-metric"><span>Team efficiency</span><b>88%</b></div></div><div className="panel" style={{ margin: "0 20px 20px", background: "rgba(0,0,0,.12)" }}><div className="panel-header"><div><div className="panel-title">Department performance</div><div className="panel-subtitle">Completed journal work by work area</div></div><Activity size={15} color="#c9c0ff" /></div>{departments.map((item, i) => <div key={item.code} style={{ display: "grid", gridTemplateColumns: "110px 1fr 38px", alignItems: "center", gap: 12, margin: "13px 0" }}><span style={{ fontSize: 10, color: "#d8d5ce" }}>{item.name}</span><div style={{ height: 7, borderRadius: 99, background: "rgba(255,255,255,.1)" }}><div style={{ height: "100%", width: `${[86, 72, 68, 61, 79][i]}%`, borderRadius: 99, background: item.color }} /></div><span className="mono">{[86, 72, 68, 61, 79][i]}%</span></div>)}</div></div>;
}

function SettingsPage() { return <div className="panel page-panel"><div className="panel-header"><div><div className="panel-title">Workspace settings</div><div className="panel-subtitle">Tune the journal to your organization</div></div><button className="primary-button"><Check size={14} /> Save changes</button></div><div className="dept-grid"><div className="dept-card"><Sparkles size={17} color="#d7cfff" /><h3 style={{ display: "block", marginTop: 13 }}>Workspace profile</h3><p>Northstar Research & Labs</p><p>Pakistan Standard Time · UTC+05:00</p></div><div className="dept-card"><Bell size={17} color="#f0d49b" /><h3 style={{ display: "block", marginTop: 13 }}>Notifications</h3><p>Daily digest at 17:30</p><p>Task reminders enabled</p></div><div className="dept-card"><Settings2 size={17} color="#bfe2b6" /><h3 style={{ display: "block", marginTop: 13 }}>Access controls</h3><p>4 administrators</p><p>Last review · 01 Sep 2026</p></div></div></div>; }

export default function Home() {
  const [page, setPage] = useState<Page>("ERP Overview");
  const [employees, setPeople] = useState<Employee[]>(employeesSeed);
  const [journal, setOperations] = useState<Operations[]>(journalSeed);
  const [tasks, setProcurement] = useState<Task[]>(taskSeed);
  const [query, setQuery] = useState("");
  const [modal, setModal] = useState<"employee" | "journal" | "task" | null>(null);
  const [toast, setToast] = useState<string | null>(null);
  const [mobileOpen, setMobileOpen] = useState(false);
    const showToast = (message: string) => { setToast(message); window.setTimeout(() => setToast(null), 2400); };
  const go = (next: Page) => { setPage(next); setMobileOpen(false); };
  const addEmployee = (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); const form = new FormData(event.currentTarget); const name = String(form.get("name") || "New employee"); const initials = name.split(" ").map(word => word[0]).join("").slice(0, 2).toUpperCase(); setPeople([{ id: Date.now(), name, initials, employeeId: `EMP-${Math.floor(100 + Math.random() * 899)}`, department: String(form.get("department") || "Laboratory"), role: String(form.get("role") || "Team member"), hours: "09:00 – 17:00", status: "Active", color: "#c9c0ff", email: String(form.get("email") || "new.member@northstar.test"), phone: "+92 300 000 0000" }, ...employees]); setModal(null); showToast(`${name} added to the team`); };
  const addOperations = (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); const form = new FormData(event.currentTarget); const employee = String(form.get("employee") || "Ahmed Khan"); setOperations([{ id: Date.now(), employee, initials: employee.split(" ").map(word => word[0]).join("").slice(0, 2), color: "#c9c0ff", department: String(form.get("department") || "Laboratory"), work: String(form.get("work") || "New work record"), time: `${String(form.get("start") || "09:00")} – ${String(form.get("end") || "10:00")}`, status: "In Progress", date: "09 Sep 2026", description: String(form.get("description") || "New record entry created from the workspace.") }, ...journal]); setModal(null); showToast("Operations entry saved"); };
  const addTask = (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); const form = new FormData(event.currentTarget); setProcurement([{ id: Date.now(), title: String(form.get("title") || "New task"), employee: String(form.get("employee") || "Ahmed Khan"), department: String(form.get("department") || "Laboratory"), due: "13 Sep 2026", priority: "Medium", status: "Pending" }, ...tasks]); setModal(null); showToast("Task created"); };
  const content = useMemo(() => { switch (page) { case "ERP Overview": return <Dashboard go={go} />; case "People": return <PeoplePage employees={employees} onAdd={() => setModal("employee")} query={query} setQuery={setQuery} />; case "Operations": return <OperationsPage entries={journal} onAdd={() => setModal("journal")} />; case "Attendance": return <AttendancePage />; case "Inventory": return <InventoryPage />; case "Procurement": return <ProcurementPage tasks={tasks} setProcurement={setProcurement} onAdd={() => setModal("task")} />; case "Finance": return <FinancePage />; case "Reports & Analytics": return <ReportsPage />; case "Settings": return <SettingsPage />; } }, [page, employees, journal, tasks, query]);
  return <div className="app-shell"><div className="top-brand"><span>Northstar</span><span>Operations</span></div><div className="top-right-mark"><i /><i /></div><aside className={`side-nav ${mobileOpen ? "open" : ""}`} aria-label="Primary navigation">{nav.map(item => { const Icon = item.icon; return <button key={item.label} className={page === item.label ? "active" : ""} onClick={() => go(item.label)} aria-label={item.label} title={item.label}><Icon size={16} /></button>; })}</aside><main className="workspace"><section className="dashboard-panel"><header className="topbar"><button className="icon-button mobile-menu" onClick={() => setMobileOpen(!mobileOpen)}><Menu size={16} /></button><nav className="nav-pills">{nav.slice(0, 7).map(item => <button key={item.label} className={`nav-pill ${page === item.label ? "active" : ""}`} onClick={() => go(item.label)}>{item.label === "Reports & Analytics" ? "Reports" : item.label}</button>)}</nav><div className="profile-chip"><div className="profile-avatar">AY</div><div className="profile-copy"><b>Ayesha Yousaf</b><span>Workspace admin</span></div><button className="profile-menu"><ChevronDown size={13} /></button></div></header>{content}</section></main>{modal && <div className="modal-backdrop" onMouseDown={e => e.target === e.currentTarget && setModal(null)}><div className="modal"><div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-start" }}><div><div className="eyebrow">Quick create</div><h2>{modal === "employee" ? "Add employee" : modal === "journal" ? "New record entry" : "Create a task"}</h2><p>Keep the record lightweight now; add detail whenever you need it.</p></div><button className="icon-button" onClick={() => setModal(null)}><X size={15} /></button></div>{modal === "employee" && <form onSubmit={addEmployee}><div className="form-grid"><div className="form-field full"><label>Full name</label><input className="input-dark" name="name" placeholder="e.g. Hira Malik" required /></div><div className="form-field"><label>Department</label><select className="select-dark" name="department" defaultValue="Laboratory"><option>Laboratory</option><option>Research</option><option>Administration</option><option>Accounts</option><option>Sales</option></select></div><div className="form-field"><label>Role</label><input className="input-dark" name="role" placeholder="Work role" /></div><div className="form-field full"><label>Email</label><input className="input-dark" name="email" type="email" placeholder="name@organization.com" /></div></div><div className="modal-actions"><button type="button" className="secondary-button" onClick={() => setModal(null)}>Cancel</button><button className="primary-button" type="submit"><Check size={14} /> Add employee</button></div></form>}{modal === "journal" && <form onSubmit={addOperations}><div className="form-grid"><div className="form-field"><label>Employee</label><select className="select-dark" name="employee" defaultValue="Ahmed Khan">{employees.map(employee => <option key={employee.id}>{employee.name}</option>)}</select></div><div className="form-field"><label>Department</label><select className="select-dark" name="department" defaultValue="Laboratory"><option>Laboratory</option><option>Research</option><option>Administration</option><option>Accounts</option><option>Sales</option></select></div><div className="form-field full"><label>Work / task</label><input className="input-dark" name="work" placeholder="e.g. Sample analysis" required /></div><div className="form-field"><label>Start time</label><input className="input-dark" name="start" defaultValue="09:00" /></div><div className="form-field"><label>End time</label><input className="input-dark" name="end" defaultValue="10:30" /></div><div className="form-field full"><label>Description</label><textarea className="textarea-dark" name="description" placeholder="What was done?" /></div></div><div className="modal-actions"><button type="button" className="secondary-button" onClick={() => setModal(null)}>Cancel</button><button className="primary-button" type="submit"><Check size={14} /> Save entry</button></div></form>}{modal === "task" && <form onSubmit={addTask}><div className="form-grid"><div className="form-field full"><label>Task title</label><input className="input-dark" name="title" placeholder="e.g. Review monthly report" required /></div><div className="form-field"><label>Assigned to</label><select className="select-dark" name="employee" defaultValue="Ahmed Khan">{employees.map(employee => <option key={employee.id}>{employee.name}</option>)}</select></div><div className="form-field"><label>Department</label><select className="select-dark" name="department" defaultValue="Laboratory"><option>Laboratory</option><option>Research</option><option>Administration</option><option>Accounts</option><option>Sales</option></select></div></div><div className="modal-actions"><button type="button" className="secondary-button" onClick={() => setModal(null)}>Cancel</button><button className="primary-button" type="submit"><Check size={14} /> Create task</button></div></form>}</div></div>}{toast && <div className="toast"><CheckCircle2 size={14} style={{ verticalAlign: "-2px", marginRight: 7 }} />{toast}</div>}</div>;
}
